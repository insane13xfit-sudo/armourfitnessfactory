# Temporary headless exporter

Run locally to capture the `#wheel` element from `spin-wheel.html` as a widescreen 4K PNG.

Usage:

```bash
cd "c:\Users\TEJA\Downloads\IT Coding and software\Spin and WIN Armour Fitness\spin-audit-20260615-104148\export-wheel-temp"
npm install
npm run export
```

The script writes `../spin-wheel-4k-widescreen.png`.
